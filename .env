# Database
DATABASE_USER=coder
DATABASE_PASSWORD=admin123

# Webhook secret — must match what your payment provider sends
# Generate with: openssl rand -hex 32
WEBHOOK_SECRET=dev-webhook-secret-change-in-production

# FX quote TTL in minutes (quotes expire after this)
FX_QUOTE_TTL_MINUTES=5

# Provider base URL (simulated in this prototype)
PROVIDER_BASE_URL=http://mock-provider
